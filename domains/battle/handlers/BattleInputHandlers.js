export function handleBuilderInput(scene) {
  return scene.handleBuilderInput?.();
}

export function handleDialogInput(scene) {
  return scene.handleDialogInput?.();
}

export function handleMainMenuInput(scene) {
  return scene.handleMainMenuInput?.();
}

export function handleSkillMenuInput(scene) {
  return scene.handleSkillMenuInput?.();
}

export function handleItemMenuInput(scene) {
  return scene.handleItemMenuInput?.();
}

export function handleItemTargetMenuInput(scene) {
  return scene.handleItemTargetMenuInput?.();
}
